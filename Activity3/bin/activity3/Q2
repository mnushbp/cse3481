Exercise 5

p is prime, therefore gcd (x, p) = 1, therefore x^psi(p) = 1 (mod p). p is prime, so psi(p) = p-1. Now, find a number d such that de = 1 (mod p-1). that d is the decryption exponent, by the same logic as
used in normal RSA decryption.